node_modules/
.DS_Store
*.crx
*.pem

# Scenarios built from real product documents, plus the source documents they
# came from. This repository is public, so anything derived from internal specs
# stays here and only fictional demos live in demo/.
demo-private/
